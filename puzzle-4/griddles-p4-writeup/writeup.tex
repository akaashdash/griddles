% ============================================================
%  Trolley Retrieval -- main writeup
%  Compile with: lualatex writeup.tex  (twice for refs)
% ============================================================
\documentclass{article}

\usepackage[final,nonatbib,main]{neurips_2026}  % final+main = non-anonymous, no line nums

% suppress NeurIPS conference footer notice
\makeatletter
\renewcommand{\@notice}{}
\makeatother

\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{array}
\usepackage{tabularx}
\usepackage{mathtools}
\usepackage{enumitem}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!60!black,
  citecolor=green!50!black,
  urlcolor=blue!60!black
}

% -- theorem environments ------------------------------------------------------
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{example}[theorem]{Example}
\newtheorem{claim}[theorem]{Claim}

% -- macros --------------------------------------------------------------------
\newcommand{\N}{\mathbb{N}}
\newcommand{\Np}{\mathbb{N}^{+}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Yes}{\textsf{Yes}}
\newcommand{\No}{\textsf{No}}
\DeclareMathOperator{\sgn}{sgn}

% small breakable typewriter for long Lean identifiers; "." and "_" become
% break opportunities (use \us for underscore, \dt for dot inside \lean).
\newcommand{\us}{\discretionary{}{}{}\_\discretionary{}{}{}}
\newcommand{\dt}{.\discretionary{}{}{}}
\newcommand{\lean}[1]{{\small\ttfamily #1}}

% -- document ------------------------------------------------------------------
\title{Trolley Retrieval:\\
A Complete, Machine-Checked Solution}

\author{%
  Akaash Dash \\
  \texttt{akaash.dash@gmail.com}
}

\begin{document}

\maketitle

% ============================================================
\begin{abstract}
We give a complete, machine-checked solution to the \emph{Trolley
Retrieval} problem (G-Research GRiddles Series~A, Puzzle~4). Writing $c$
for the permutation of the positive integers that labels the tape, the
answer is
\[
  \boxed{\;\text{Bob has a winning strategy}\iff
  c\text{ is \emph{not} eventually the identity}\;}
\]
that is, $c(n)\neq n$ for infinitely many~$n$ (equivalently, $c$ is not
finitary). The losing direction is a parity lock that makes consecutive
cells permanently indistinguishable once $c$ has an identity tail. The
winning direction builds an explicit, signal-independent exploration
strategy, splitting on whether the displacement $c(n)-n$ is bounded
above: a single fixed ``lag'' recurs when it is, whereas in the
unbounded-above regime the natural single-lag recurrence is
\emph{provably false}, so a growing-lag staircase tailored to~$c$ is used
instead. Every step is formalised in \textbf{Lean~4}: the main theorem is
fully \lean{sorry}-free and axiom-clean, its \verb|#print axioms| audit
listing only the three standard foundational axioms (\verb|propext|,
\verb|Classical.choice|, \verb|Quot.sound|).
\end{abstract}

% ============================================================
\section{The problem}\label{sec:problem}

The exact statement is as follows. Let $c(1), c(2), \dots$ be a sequence
of positive integers in which every positive integer appears exactly
once; equivalently $c$ is a permutation (bijection) of $\Np$. A one-way
infinite sequence of cells is given: cell~$n$ (the $n$-th from the left,
$n\ge 1$) is labelled~$c(n)$.

At time $t=0$ an invisible trolley is placed on an unknown cell, the
$k_0$-th from the left. For each integer $t\ge 1$, in order:
\begin{enumerate}[topsep=2pt,itemsep=1pt]
  \item Bob chooses a direction, left or right; the trolley moves one
  cell that way, to cell $k_t$.
  \item The trolley sends \Yes{} if $c(k_t) < t$, and \No{} otherwise.
\end{enumerate}
If ever $k_t = 0$ (the trolley falls off the left end), Bob loses
immediately. At any time Bob may instead \emph{guess} the trolley's
position; a correct guess retrieves it, an incorrect guess loses.

\begin{quote}
\itshape Determine all permutations $c$ for which Bob has a strategy
guaranteeing safe retrieval.
\end{quote}

Bob knows $c$ in full; the only hidden datum is the start cell $k_0$. The
trolley is invisible---Bob never sees $k_t$ directly. His sole feedback
is the binary signal, and he chooses each move adaptively from the
signal history.

% ------------------------------------------------------------
\subsection{The answer}

\begin{theorem}[Main]\label{thm:main}
Bob has a strategy ensuring safe retrieval if and only if $c$ is
\emph{not eventually the identity}; that is, if and only if
$c(n)\neq n$ for infinitely many~$n$.
\end{theorem}

\[
  \boxed{\;\textbf{Bob wins from every start cell} \iff
  c(n)\neq n \text{ for infinitely many } n.\;}
\]

We say $c$ is \emph{eventually identity} (EI) if there is a threshold
$N$ with $c(n)=n$ for all $n>N$. Since $c$ is a bijection, ``not EI'' has
several equivalent forms, all used below:
\begin{itemize}[topsep=2pt,itemsep=1pt]
  \item $\{\,n : c(n)\neq n\,\}$ is infinite ($c$ is not finitary);
  \item $c^{-1}$ is not eventually identity (the condition is symmetric
  under $c\leftrightarrow c^{-1}$);
  \item $c$ has infinitely many \emph{upward gaps}: positions $p$ with
  $c(p{+}1)-c(p)\ge 2$.
\end{itemize}
The last equivalence is worth recording, as it is exactly what fails in
the EI case.

\begin{proposition}\label{prop:gaps}
$c$ has infinitely many upward gaps if and only if $c$ is not eventually
identity.
\end{proposition}
\begin{proof}
If $c(n{+}1)\le c(n)+1$ for all $n>N$ (no gaps past $N$), then on
$(N,\infty)$ the bijection $c$ is non-decreasing by steps of at most~$1$;
a value-preserving bijection of a tail of $\Np$ that never
jumps up by~$2$ cannot skip a value and cannot repeat one, forcing
$c(n)=n$ for all $n>N$. The contrapositive is the claim.
\end{proof}

% ------------------------------------------------------------
\subsection{The game model}\label{sec:model}

We record the model precisely, since the formalisation depends on it.
Positions are integers; the start cell $k_0\in\Np$ is a positive integer
but a trajectory may, in principle, drive a position to $\le 0$, which
loses. A \emph{strategy} $\sigma$ maps a signal history (most-recent
first) to an \emph{action}: either a move (\textsf{left}/\textsf{right})
or a guess of a cell. From a start $k_0$, the strategy determines a
sequence of states $(\,\text{position}_t,\ \text{history}_t\,)$; the
signal appended at time~$t$ is \Yes{} iff the (valid) new position~$p$
satisfies $c(p)<t$. Bob \emph{wins from $k_0$} if there is a finite stop
time~$T$ such that (i) every visited position through~$T$ is a valid cell
($\ge 1$), (ii) every action before~$T$ is a move, and (iii) at time~$T$
the strategy guesses exactly the current position $k_0 + D_T$, where
$D_T$ is Bob's net displacement. \emph{Bob wins} (\lean{BobWins}~$c$)
if a single strategy wins from \emph{every} start cell.

% ------------------------------------------------------------
\subsection{The alarm-clock view and the parity lock}
\label{sec:reform}

It is convenient to read the labels as wake-up times. Think of cell~$n$
as carrying an alarm clock set for time $c(n)$: at every time
$t > c(n)$ the cell is ``awake'', and a trolley standing on it reports
\Yes; while $t \le c(n)$ the cell is ``asleep'' and reports \No. Because
$c$ is a bijection, exactly one cell wakes at each integer time, and
every cell wakes eventually. This is just a restatement of
$\text{signal}=\Yes \iff c(k_t) < t$.

Let $D_t := k_t - k_0$ be Bob's net displacement at time~$t$.

\begin{lemma}[Parity lock]\label{lem:parity}
As long as Bob has not yet guessed, $D_t \equiv t \pmod 2$. Equivalently,
$t - D_t$ is always even.
\end{lemma}
\begin{proof}
$D_0=0$, and each move changes $D$ by $\pm 1$ and $t$ by $+1$, so the
parity of $t-D_t$ is invariant; it starts even.
\end{proof}

The parity lock is the single structural fact behind the losing
direction. A second, equally elementary identity exposes \emph{why}.
Let $L_t$ be the number of \emph{left} moves Bob has made by time~$t$ and
$R_t$ the number of right moves, so $t=L_t+R_t$ and $D_t=R_t-L_t$. Then
\begin{equation}\label{eq:tD}
  t - D_t \;=\; (L_t+R_t) - (R_t-L_t) \;=\; 2L_t .
\end{equation}
Write $g(m) := c(m)-m$ for the displacement of the label from the
diagonal. The signal at time~$t$ is
\begin{align}
  \text{\Yes}
  &\iff c(k_0 + D_t) < t
  \iff g(k_0+D_t) < t - D_t - k_0 \notag\\
  &\iff g(k_0+D_t) < 2L_t - k_0 . \label{eq:Lthreshold}
\end{align}
In other words, the walk reads the sequence~$g$ against a threshold
$2L_t - k_0$ that Bob controls only through his \emph{left-move count}.

\begin{remark}[The whole obstruction, in one line]\label{rem:flat}
For the identity, $g\equiv 0$, so~\eqref{eq:Lthreshold} collapses to
$\Yes \iff k_0 < 2L_t$. Bob can only ever compare $k_0$ against
\emph{even} thresholds, and $2j < 2L \iff 2j{+}1 < 2L$ for every~$L$.
Hence the start cells $2j$ and $2j{+}1$ produce \emph{identical signals
under every strategy}: the parity bit of $k_0$ is unrecoverable. This is
the seed of the losing direction; the work below is to extend it to every
eventually-identity~$c$ despite the perturbed initial segment.
\end{remark}

% ------------------------------------------------------------
\subsection{Bob's strategy, in plain terms}\label{sec:strategy}

Before the two directions, here is exactly what Bob does to win when $c$ is
not eventually the identity. The strategy has two independent parts: a fixed
exploration walk that he plays \emph{blind}, and a stopping rule driven by
the signals.

\paragraph{The exploration walk (predetermined, $c$-tailored).}
From his knowledge of $c$ alone, Bob fixes \emph{in advance} an infinite
sequence of left/right pushes---the \emph{staircase}---with one defining
property: the net displacement is never negative, $D_t\ge 0$ for all~$t$.
Since the trolley then sits at $k_0+D_t\ge k_0\ge 1$, it \emph{never falls
off the tape}, whatever the unknown start~$k_0$---in particular it is safe
even when $k_0=1$, the tightest case. The staircase sweeps through the
ordered pairs of start cells $(a,b)$, $a<b$, in a fixed order; for each
pair it
\begin{enumerate}[topsep=2pt,itemsep=1pt]
  \item \emph{raises its lag} $\ell=t-D$ to a target value by oscillating
  in place (a right push then a left push bumps $\ell$ by~$2$ and returns
  $D$ to where it was);
  \item \emph{rides right} until the displacement hits a \emph{separator}
  for $(a,b)$---a moment at which the trolley's report would be \Yes{} if
  the start were~$a$ but \No{} if it were~$b$ (or vice versa); then
  \item \emph{tails right}, so displacements grow without bound across
  phases.
\end{enumerate}
When $c$'s labels do not run away upward (\emph{bounded above}) the target
lag is one fixed value per pair; when they do (\emph{unbounded above}) the
target lag must grow from pair to pair. Either way it is a single, fixed,
signal-independent sequence: Bob never consults the signals to decide a
move.

\paragraph{The stopping rule (signal-driven).}
While walking, Bob maintains a \emph{belief}: the set of start cells~$k$
whose \emph{hypothetical} signal history---which he can compute, knowing
$c$ and his own moves---matches the signals he has actually heard.
Initially every cell is possible; each signal that disagrees with a
candidate's hypothetical eliminates it. A single \Yes{} already cuts the
belief down to a \emph{finite} set (only finitely many cells can be awake
at any one moment), and because the staircase visits a separator for
\emph{every} pair, distinct starts eventually yield distinct histories, so
the belief keeps shrinking. The instant it is a single cell~$k_0$, Bob
stops, computes the trolley's current position $k_0+D_t$ from~$k_0$ and his
own (known) displacement, and guesses it---retrieving the trolley.

In one sentence: \emph{play a fixed, safe, rightward staircase, and stop to
guess the moment the signals have pinned down the start cell.}
Section~\ref{sec:winning} makes good on both claims: that such a staircase
exists (\S\ref{sec:bounded}, \S\ref{sec:unbounded}), and that the stopping
time is always finite (\S\ref{sec:reduction}).

% ============================================================
\section{Losing direction: eventually identity \texorpdfstring{$\Rightarrow$}{implies} Bob loses}
\label{sec:losing}

\begin{theorem}[\lean{EventuallyIdentity\us loses}]\label{thm:losing}
If $c$ is eventually identity, Bob has no strategy that wins from every
start cell.
\end{theorem}

The proof isolates a structural ``parallel-evolution'' lemma and then
verifies its hypothesis for two carefully chosen start cells.

% ------------------------------------------------------------
\subsection{Parallel evolution}

Fix a strategy~$\sigma$. For a start cell $k$ let $\mathrm{pos}(k,t)$ be
the position and $\mathrm{hist}(k,t)$ the signal history (most-recent
first) at time~$t$. Bob's action at any decision point is a function of
$\mathrm{hist}$ alone.

\begin{lemma}[Parallel evolution; \lean{parallel\us evolution}]\label{lem:parallel}
Let $k_1, k_2$ be two start cells. Suppose that, started from~$k_1$, Bob
makes no guess before time~$t$, and that at every time $s\le t$ the
signal observed from $k_1$'s trajectory equals the signal observed from
$k_2$'s trajectory. Then the two histories agree at time~$t$, and the
positions stay rigidly offset:
\[
  \mathrm{hist}(k_1,t) = \mathrm{hist}(k_2,t),
  \qquad
  \mathrm{pos}(k_2,t) = \mathrm{pos}(k_1,t) + (k_2-k_1).
\]
\end{lemma}
\begin{proof}
Induction on~$t$. At $t=0$ the histories are empty and the offset is
$k_2-k_1$ by definition. For the step, equal histories make $\sigma$
choose the \emph{same} action in both worlds; since Bob has not guessed,
that action is a move, applied identically to both, preserving the
offset. The new signal entries are equal by the time-$(t{+}1)$ hypothesis,
so the histories remain equal.
\end{proof}

\begin{corollary}\label{cor:notboth}
If $k_1\neq k_2$ and the two signal traces agree at \emph{every} time
under~$\sigma$, then $\sigma$ cannot win from both $k_1$ and $k_2$.
\end{corollary}
\begin{proof}
Equal traces force equal histories at all times (Lemma~\ref{lem:parallel}
applied up to either guess time), hence equal first-guess times
$T_1=T_2=:T$ and equal guesses $g_1=g_2$. But correctness would require
$g_1 = \mathrm{pos}(k_1,T)$ and $g_2=\mathrm{pos}(k_2,T)$, while
$\mathrm{pos}(k_2,T)=\mathrm{pos}(k_1,T)+(k_2-k_1)$. These contradict
$k_1\neq k_2$.
\end{proof}

% ------------------------------------------------------------
\subsection{The indistinguishable pair for eventually-identity \texorpdfstring{$c$}{c}}

Suppose $c(n)=n$ for all $n>N$. Let $M$ be an upper bound for the finitely
many perturbed labels: $M \ge \max\{\,c(m) : 1\le m\le N\,\}$ and $M\ge
N+1$. Choose $j$ with $2j > M$ and set
\[
  k_1 = 2j, \qquad k_2 = 2j+1 .
\]

\begin{lemma}[Signal match; \lean{signal\us match}]\label{lem:sigmatch}
Fix a no-guess strategy run from $k_1=2j$. At every time~$t$, every
position~$p\ge 1$ reachable from $2j$ in $t$ moves (so $2j - t \le p$)
that respects the parity lock ($t+p$ even) satisfies
\[
  \text{signal at } p \text{ at time } t \;=\;
  \text{signal at } p{+}1 \text{ at time } t .
\]
\end{lemma}
\begin{proof}
Two cases on the location~$p$.

\emph{(a) $p > N$ (identity region).} Both $p$ and $p{+}1$ exceed~$N$, so
$c(p)=p$ and $c(p{+}1)=p{+}1$. The signal at $p$ is $\Yes \iff p<t$, and
at $p{+}1$ is $\Yes\iff p{+}1<t$. These differ only if $t=p+1$. But then
$t+p = 2p+1$ is odd, violating the parity hypothesis $t+p$ even. So the
signals agree.

\emph{(b) $p \le N$ (perturbed region).} Here the trolley has travelled
from $2j$ to $p\le N$, so $t \ge 2j - p \ge 2j - N$; concretely $2j>M\ge
N+1$ forces $t > M$. Both $c(p)$ and $c(p{+}1)$ are at most~$M$ (using
$c(N{+}1)=N{+}1\le M$ for the boundary), hence $c(p), c(p{+}1) < t$: both
signals are \Yes. They agree.
\end{proof}

The two cases capture the dichotomy of Remark~\ref{rem:flat}: in the
identity tail, the parity lock makes $2j$ and $2j{+}1$ collide; in the
perturbed segment, the time cost of reaching it ($t\ge 2j-N$) is so large
that every cell there is already awake, so both worlds report \Yes{} and
again collide.

\begin{proof}[Proof of Theorem~\ref{thm:losing}]
Suppose $\sigma$ wins from every start cell, in particular from
$k_1=2j$ and $k_2=2j{+}1$ above. We claim the two signal traces agree at
every time, which contradicts Corollary~\ref{cor:notboth}.

By induction on~$t$, the trajectories from $2j$ and $2j{+}1$ evolve in
lockstep with positions offset by exactly~$1$ and equal histories, as
long as Bob has not guessed: the inductive step applies the signal match
(Lemma~\ref{lem:sigmatch}) at $\mathrm{pos}(2j,t{+}1)$, whose
hypotheses---validity $p\ge 1$ (Bob wins, so the trajectory is safe),
reachability $2j-t\le p$ (a $\pm1$ walk), and parity
(Lemma~\ref{lem:parity})---all hold. Hence the signal entering both
histories at each step is identical, so
$\mathrm{hist}(2j,t)=\mathrm{hist}(2j{+}1,t)$ and
$\mathrm{pos}(2j{+}1,t)=\mathrm{pos}(2j,t)+1$ for all~$t$. The traces
agree at every time, and Corollary~\ref{cor:notboth} gives the
contradiction.
\end{proof}

\begin{remark}[The half-infinite tape is irrelevant here]
For the chosen large $k_0\approx 2j$, no finite-time strategy can reach
cell~$0$, so the ``falling off'' rule never binds; the indistinguishability
argument is unaffected by the absence of a left end.
\end{remark}

% ============================================================
\section{Winning direction: not eventually identity \texorpdfstring{$\Rightarrow$}{implies} Bob wins}
\label{sec:winning}

The winning direction has three layers, treated in
Sections~\ref{sec:distinguish}--\ref{sec:dichotomy}. First, a
\emph{distinguishability theorem}: when $c$ is not eventually identity,
\emph{no} pair of start cells can stay indistinguishable. Second, a
generic \emph{reduction}: any single signal-independent trajectory that is
safe, informative, and pairwise-separating is already a winning strategy.
Third, an explicit construction of such a trajectory, which splits into
two regimes according to whether $c$'s displacement is bounded above.

% ------------------------------------------------------------
\subsection{The distinguishability theorem}\label{sec:distinguish}

Reaching displacement~$D$ from the start costs time at least~$|D|$, and
the parity lock forces $t\equiv D\pmod 2$. Within those constraints Bob
can pad with oscillation to reach any admissible time, and (because a cell
once awake stays awake) the only information at displacement~$D$ is the
\emph{set of times $t\ge D$, $t\equiv D\pmod 2$, at which the cell is
awake}---equivalently, the single comparison $c(a+D) < t$. This motivates:

\begin{definition}[Indistinguishable pair; \lean{Indistinguishable}]\label{def:indist}
Two start cells $a<b$ are \emph{indistinguishable} (for~$c$) if for every
displacement $D\ge 0$ and every time $t\ge D$ with $t\equiv D\pmod 2$,
\[
  c(a+D) < t \iff c(b+D) < t .
\]
\end{definition}

Quantifying over \emph{all} reachable $(D,t)$---including small times that
catch early wake-ups---is essential; it is exactly the freedom that lets
Bob's oscillation observe a cell's wake-up time at full resolution.

\begin{theorem}[Distinguishability;
  \lean{Indistinguishable\us implies\us eventually\us identity},
  \lean{not\us eventually\us identity\us implies\us distinguishable}]
\label{thm:lemmaA}
If $a<b$ are indistinguishable for~$c$, then $b=a+1$ and $c(n)=n$ for all
$n\ge a$. Consequently, if $c$ is not eventually identity, then \emph{no}
pair of start cells is indistinguishable.
\end{theorem}

We prove the first sentence; the second is its contrapositive (an
indistinguishable pair would exhibit an identity tail, making $c$ EI).
Throughout write $\Delta := b-a \ge 1$.

The proof has two regimes by the size of~$\Delta$. The case $\Delta=1$ is
a clean shift argument; the case $\Delta\ge 2$ is the crux. We first
extract the local structure shared by both.

\begin{lemma}[Consecutiveness; \lean{indist\us consec}]\label{lem:consec}
Call a displacement~$D$ \emph{active} (for the cell~$a$) if
$c(a+D)\ge D$, and similarly for~$b$. If $a<b$ are indistinguishable and
$D$ is active for both $a$ and $b$, then $c(a+D)$ and $c(b+D)$ are
consecutive integers: $|\,c(a+D) - c(b+D)\,| = 1$.
\end{lemma}
\begin{proof}
Let $X=c(a+D)$, $Y=c(b+D)$. Injectivity gives $X\neq Y$; say $X<Y$. If
$Y\ge X+2$, pick~$t$ with $X<t\le Y$ of the parity $t\equiv D\pmod 2$
(possible because the interval $(X,Y]$ has length $\ge 2$ and so contains
both parities). Then $c(a+D)<t$ but $c(b+D)\not<t$, with $t\ge D$ (as $D$
is active and $t>X\ge D$), contradicting indistinguishability at $(D,t)$.
Hence $Y=X+1$. The case $Y<X$ is symmetric.
\end{proof}

\begin{lemma}[Bad displacements are forward-closed;
  \lean{bad\us D\us propagates}]\label{lem:badprop}
Call $D$ \emph{bad} (for~$a$) if $c(a+D) < D$. If $a<b$ are
indistinguishable and $D$ is bad, then $D+\Delta$ is bad:
$c(a+D+\Delta) < D+\Delta$.
\end{lemma}
\begin{proof}
$D$ bad means the cell $a+D$ is awake at the least admissible time
$t\le D+1$ with $t\equiv D \pmod 2$, so its signal is \Yes.
Indistinguishability transfers \Yes{} to $b+D = a+(D+\Delta)$:
$c(a+D+\Delta) < t \le D+1$, and since the value is an integer
$c(a+D+\Delta)\le D < D+\Delta$. (The point is purely that an
already-saturated \Yes{} propagates by $+\Delta$; this is where the
constraint $b=a+\Delta$ enters.)
\end{proof}

Lemma~\ref{lem:badprop} is decisive: on each residue class modulo
$\Delta$, the bad displacements form an \emph{up-set}, so the
\emph{active} (non-bad) displacements form a finite or cofinite
\emph{prefix}---never an interleaving of good and bad. With it, the crux
is short.

\begin{lemma}[Chain bound; \lean{chain\us bound}]\label{lem:chain}
If $a<b$ are indistinguishable, fix a residue $r$ with $0\le r<\Delta$.
For any~$k$ such that $r, r+\Delta, \dots, r+k\Delta$ are all active,
$c(a + r + k\Delta) \le c(a+r) + k$.
\end{lemma}
\begin{proof}
By Lemma~\ref{lem:consec}, consecutive active displacements on the
residue satisfy $|c(a + r + (i{+}1)\Delta) - c(a + r + i\Delta)| = 1$
(apply consecutiveness at the displacement $r+i\Delta$, whose
$+\Delta$ shift lands at $b + r + i\Delta$). Summing $k$ unit steps and
the triangle inequality give $c(a+r+k\Delta)\le c(a+r)+k$.
\end{proof}

\begin{lemma}[$\Delta\ge 2$ is impossible;
  \lean{indist\us Delta\us ge\us 2\us impossible}]\label{lem:Delta2}
If $a<b$ are indistinguishable with $\Delta=b-a\ge 2$, contradiction.
\end{lemma}
\begin{proof}
Let $M := \max_{0\le r<\Delta} c(a+r)$. Suppose a displacement $D$ is
active, and write $D=r+k\Delta$ with $0\le r<\Delta$. By forward-closure
(Lemma~\ref{lem:badprop}), \emph{all} of $r, r+\Delta, \dots, r+k\Delta$
are active (active is a prefix on each residue). Activity gives
$c(a+D)\ge D = r+k\Delta$, while the chain bound gives
$c(a+D)\le c(a+r)+k \le M+k$. Hence
\[
  r + k\Delta \le M + k
  \quad\Longrightarrow\quad
  k(\Delta-1) \le M - r \le M .
\]
With $\Delta\ge 2$ this bounds $k\le M$, so $D = r+k\Delta < (M{+}1)\Delta$.
Thus \emph{every} displacement $D \ge (M{+}1)\Delta$ is bad:
$c(a+D) < D$ for all such~$D$.

This forces a pigeonhole collapse. For $D \ge (M{+}1)\Delta$, the cell at
position $a+D$ has label $c(a+D) < D = (a+D) - a$, i.e.\ $c(p) < p-a$ for
all large~$p$. The finitely many small positions have labels bounded by
some constant. Choosing $N$ large enough, $c$ maps the segment $[1,N]$
into $[1, N-a-1]$: large positions $p$ land below $p-a \le N-a-1$, and
the bounded small positions also fit once $N$ is large. But $[1,N]$ has
$N$ elements and $[1, N-a-1]$ has $N-a-1 < N$, so $c$ cannot be injective
on $[1,N]$. Contradiction.
\end{proof}

\begin{remark}[Why this is the right proof]
The contradiction in Lemma~\ref{lem:Delta2} uses \emph{no} parity
argument and \emph{no} even/odd split on~$\Delta$. The single lever is
forward-closure of badness (Lemma~\ref{lem:badprop}), which turns the
``active'' set on each residue into a prefix; the chain bound then makes
that prefix finite for $\Delta\ge 2$, and a one-line pigeonhole finishes.
This is markedly simpler than the even/odd case analysis we initially
attempted, and it was the breakthrough that turned the crux from
``research-level'' into elementary.
\end{remark}

It remains to handle $\Delta=1$.

\begin{lemma}[$\Delta=1$ forces an identity tail;
  \lean{shift\us one\us implies\us eventually\us identity}]\label{lem:Delta1}
If $a<b=a+1$ are indistinguishable, then $c(n)=n$ for all $n\ge a$.
\end{lemma}
\begin{proof}
For $\Delta=1$, $b+D = a+(D+1)$, so consecutiveness
(Lemma~\ref{lem:consec}) on active displacements reads
$|c(a+D+1)-c(a+D)|=1$: the labels along $a, a{+}1, a{+}2, \dots$ form a
self-avoiding $\pm 1$ walk. Let $\sigma(0)=\sgn\bigl(c(a{+}1)-c(a)\bigr)$
be the sign of the first step. If $\sigma(0)=-1$ (descending start), the
walk $c(a), c(a){-}1, c(a){-}2,\dots$ would eventually drop below~$1$,
impossible for labels in $\Np$; so $\sigma(0)=+1$. A self-avoiding $\pm 1$
walk that starts ascending must ascend forever (any later descent would
revisit a value, breaking injectivity), giving $c(n+1)=c(n)+1$ for all
$n\ge a$, i.e.\ $c(n)=n + C$ on $[a,\infty)$ with $C=c(a)-a$. If $C>0$,
the values $\{1,\dots,C\}$ are absent from $c([a,\infty))$ and must be
supplied by the finite set $c([1,a{-}1])$, which has only $a-1$ elements;
bijectivity of $c$ on $\Np$ then forces $C=0$ (and $C<0$ was already
excluded). Hence $c(n)=n$ for $n\ge a$.
\end{proof}

\begin{proof}[Proof of Theorem~\ref{thm:lemmaA}]
Let $a<b$ be indistinguishable, $\Delta=b-a$. By Lemma~\ref{lem:Delta2},
$\Delta\ge 2$ is impossible, so $\Delta=1$, i.e.\ $b=a+1$; then
Lemma~\ref{lem:Delta1} gives $c(n)=n$ for $n\ge a$, so $c$ is eventually
identity. Contrapositively, if $c$ is not eventually identity then no
pair is indistinguishable.
\end{proof}

% ------------------------------------------------------------
\subsection{The reduction: separation \texorpdfstring{$+$}{+} safety \texorpdfstring{$+$}{+} Yes \texorpdfstring{$\Rightarrow$}{implies} Bob wins}
\label{sec:reduction}

Theorem~\ref{thm:lemmaA} says only that every pair of cells admits
\emph{some} separating observation. Turning this into a single winning
strategy is the substantial part of the problem. The key conceptual move
is that Bob does not need an \emph{adaptive} (signal-dependent) move
schedule at all: a fixed, signal-independent exploration trajectory
suffices, provided it is tailored to~$c$. He plays the trajectory blind,
using the signals only to decide \emph{when to stop and guess}.

Fix a deterministic move sequence $m\colon \N\to\{\mathrm{L},\mathrm{R}\}$
and write $\mathrm{pos}_m(k_0,t)$ for its position from start~$k_0$. The
strategy \lean{ofMoves}~$m$ replays $m$'s moves blindly. Call the
trajectory:
\begin{itemize}[topsep=2pt,itemsep=1pt]
  \item \emph{safe} if $\mathrm{pos}_m(k_0,t)\ge 1$ for all $k_0,t$
  (never falls off the left);
  \item \emph{Yes-emitting} if from every $k_0$ some time emits \Yes;
  \item \emph{separating} if for every $a\neq b$ the signal histories from
  $a$ and from $b$ eventually differ.
\end{itemize}

\begin{theorem}[Reduction; \lean{localizes\us BobWins}, \lean{ofMoves\us localizes}]
\label{thm:reduction}
If a fixed trajectory $m$ is safe, Yes-emitting, and separating, then Bob
wins from every start cell.
\end{theorem}

The nontrivial point is that separation is an \emph{infinite} family of
conditions (one per wrong candidate), whereas a win requires a
\emph{single finite} stop time~$T$ that has ruled out \emph{all} wrong
candidates at once. The bridge is first-Yes finiteness.

\begin{lemma}[First-Yes finiteness; \lean{yesSet\us finite}]
\label{lem:yesfin}
For any time~$s$ and displacement-offset $\delta$, the set of start cells
$k$ whose signal at time~$s$ (after the shift~$\delta$) is \Yes{} is
\emph{finite}.
\end{lemma}
\begin{proof}
A \Yes{} at $(s,\delta)$ from start~$k$ means $c(k+\delta) < s$. As $c$
is a bijection, only the finitely many values $\{1,\dots,s-1\}$ lie below
the threshold, so only finitely many cells $k+\delta$ (hence finitely
many~$k$) can emit \Yes{} at that moment.
\end{proof}

\begin{proof}[Proof of Theorem~\ref{thm:reduction}]
Fix the true start~$k_0$. By Yes-emission there is a first time~$s_0$ at
which $k_0$'s trajectory reads \Yes. Any candidate~$k$ \emph{not yet
distinguished} from $k_0$ by time~$s_0$ must read the \emph{same} signal,
hence also \Yes, at~$s_0$; by Lemma~\ref{lem:yesfin} there are only
finitely many such~$k$. Each of these finitely many candidates is
separated from~$k_0$ at \emph{some} finite time (separation), so the
maximum~$T$ of those finitely many separation times is finite, and by
time~$T$ \emph{every} wrong candidate has produced a differing signal.
Bob plays $m$ until~$T$; the histories now pin~$k_0$ uniquely. Knowing
$k_0$ and his own (signal-independent) displacement $D_T$, Bob computes
the current cell $k_0 + D_T$ and guesses it. Safety guarantees every
visited position was a valid cell, so the guess is timely and correct.
\end{proof}

In Lean, the ``stop and guess'' wrapper is the belief-state strategy
\lean{beliefStrat}: it replays $m$'s moves until the set of
signal-consistent candidates is a singleton, then guesses. The match
between the wrapped and bare trajectories up to the first resolved time,
and the correctness of the guess, are discharged in
\lean{localizes\us BobWins}. \emph{All trajectories we construct keep
$D\ge 0$ at all times}, so safety is automatic: $\mathrm{pos} = k_0 + D
\ge k_0 \ge 1$. What remains is to exhibit one fixed trajectory that is
separating and Yes-emitting.

% ------------------------------------------------------------
\subsection{Lag monotonicity and why a single fixed lag fails}
\label{sec:lag}

The trajectory's structure is governed by one invariant. Define the
\emph{lag} $\ell_t := t - D_t = 2L_t$ (twice Bob's left-move count,
by~\eqref{eq:tD}). Since each move changes $D$ by $\pm1$ and $t$ by $+1$,
\[
  \ell_{t+1}-\ell_t \in \{0, 2\}:
\]
the lag is \textbf{monotone nondecreasing and never returns to a smaller
value}. By the alarm-clock identity~\eqref{eq:Lthreshold}, a separating
observation for a pair $(a,b)$ at displacement~$D$ lives at a definite
even \emph{slack} $s = t-D = \ell_t$, namely the event
\[
  \bigl[\,c(a+D) < D+s\,\bigr] \;\neq\; \bigl[\,c(b+D) < D+s\,\bigr].
\]
Because the lag only increases, a separator at slack~$s$ is
\emph{use-it-or-lose-it}: once Bob's lag has passed~$s$, he can never
again present that pair with that slack. He must therefore visit
separators in nondecreasing slack order. This single fact dictates the
whole construction.

The natural hope is that, for each pair $(a,b)$, separators \emph{recur}
at one \emph{fixed} even slack $s(a,b)$: there are infinitely many
displacements~$D$ at which $(a,b)$ is separated at the \emph{same}
slack~$s$. If that held universally, Bob could fix a slack per pair and
ride right to catch a recurrence. We call this the \emph{band recurrence}.
It is tempting but \textbf{false}.

\begin{remark}[The band recurrence is false: the AP-split counterexample]
\label{rem:apsplit}
Consider the permutation that splits $\Np$ into an arithmetic-progression
shape: on the even cells, $c(2k) = 2k + 2\lfloor\sqrt{k}\rfloor$ (the even
labels ascend slowly, with displacement $g\to\infty$), and the odd cells
fill the remaining values in increasing order (their displacement drifts
to $-\infty$). This is a genuine non-eventually-identity bijection. For
the pair $(2,4)$ the separating slack \emph{climbs} like $\sqrt{D}$: at
even displacement $D$ the separating window is roughly
$(2+2\sqrt{D},\,4+2\sqrt{D}]$, of width $\approx 2$, escaping upward. Any
fixed even slack $s$ sits inside that window only for $D$ in a bounded
range and is then abandoned forever as the window climbs. Hence \emph{no}
fixed slack recurs at unbounded displacement, and the band recurrence
fails. Bob still wins---the separators exist, they merely sit at a
\emph{growing} slack---but only a trajectory that raises its lag without
bound can catch them. This is the concrete realisation of the principle
that no single fixed move schedule is universal, and it motivates the
dichotomy of the next section.
\end{remark}

% ------------------------------------------------------------
\subsection{The dichotomy: a \texorpdfstring{$c$}{c}-tailored trajectory in each regime}
\label{sec:dichotomy}

Whether the band recurrence holds turns out to be governed by a single
structural property of~$c$. Say $c$ is \emph{bounded above} if there is a
constant $B$ with $c(n)\le n+B$ for all~$n$ (equivalently, $g(n)=c(n)-n$
is bounded above). The main theorem's winning direction dispatches on
this property (\lean{main}'s \lean{by\us cases} in \lean{Answer\dt lean}):
\begin{itemize}[topsep=3pt,itemsep=4pt,leftmargin=*]
  \item \textbf{Bounded above.} The band recurrence \emph{holds}: each
  pair has a fixed recurring even slack, and a lag-monotone
  \emph{fixed-lag staircase} catches it (Section~\ref{sec:bounded}).
  \item \textbf{Unbounded above.} The band recurrence may fail
  (Remark~\ref{rem:apsplit}), but separators exist at \emph{arbitrarily
  large} even slack, and a $c$-tailored \emph{growing-lag staircase}
  visits one per pair, raising its lag monotonically
  (Section~\ref{sec:unbounded}).
\end{itemize}
In both regimes the trajectory is a single, predetermined,
signal-independent move sequence with $D\ge 0$ throughout, fed to the
reduction (Theorem~\ref{thm:reduction}). No adaptive belief-state
strategy is needed.

\subsubsection{Bounded above: the fixed-lag staircase}
\label{sec:bounded}

The combinatorial input here is the band recurrence at a $\max$-bounded
slack.

\begin{theorem}[Band recurrence under bounded displacement;
  \lean{band\us of\us boundedAbove}]\label{thm:bandrec}
If $c$ is not eventually identity and is bounded above, then for any
distinct $a,b$ there is an \emph{even} slack $s\ge\max(a,b)$ whose
slack-$s$ separators recur at arbitrarily large displacement:
$\forall D_0\,\exists D\ge D_0$ with
$[\,c(a{+}D)<D{+}s\,]\neq[\,c(b{+}D)<D{+}s\,]$.
\end{theorem}
\begin{proof}[Proof idea]
Work with the integer displacement $f(n)=c(n)-n$. If the band failed at
every even slack $s\ge\max(a,b)$, then past some finite floor the two
displaced signals would \emph{agree} for all~$D$
(\lean{signals\us agree\us of\us no\us separator}, via the window
description $\min<D{+}s\le\max$). Boundedness above, combined with
infinitely many weak ascents (\lean{weak\us ascents\us infinite}), pins a
\emph{top recurring value} $v^\star\ge 0$ for~$f$. If $v^\star=0$ then
$f(n)\le 0$ cofinitely, i.e.\ $c(n)\le n$ for all large~$n$, and the cut
lemma below forces $c$ eventually identity---contradiction. If
$v^\star\ge 1$, the agreement relation, propagated along the residue ray
modulo $\Delta=b-a$, contradicts the recurrence of $v^\star$. Either way
the band holds.
\end{proof}

The cut lemma used above is the elegant heart of this regime and is worth
isolating.

\begin{lemma}[Cut lemma; \lean{descent\us cofinite\us imp\us EI}]\label{lem:cut}
If $c(n)\le n$ for all $n\ge N$, then $c$ is eventually identity.
\end{lemma}
\begin{proof}
Let $B$ bound $c$ on the finite segment $\{1,\dots,N\}$. For any
$M\ge\max(N,B)$, the descent hypothesis past~$N$ and the bound below~$N$
together show $c$ maps $\{1,\dots,M\}$ \emph{into itself}; being an
injection of a finite set into itself, it is \emph{onto} $\{1,\dots,M\}$.
Hence $c(M{+}1)$ cannot land in $\{1,\dots,M\}$ (those values are already
hit), so $c(M{+}1)>M$; with $c(M{+}1)\le M{+}1$ from descent, $c$
fixes~$M{+}1$. Applying this for every $M=n{-}1$ past the threshold gives
EI. (The ascent-cofinite mirror follows by applying this to $c^{-1}$.)
\end{proof}

\paragraph{The staircase.}
Given the recurring slacks, the trajectory is assembled as a
\emph{band-top lag-monotone staircase} (\lean{BandProvider},
\lean{BobWins\us of\us bandProvider}). Enumerate the ordered distinct
pairs $(a,b)$, $a<b$, by the key $\bigl(s(a,b),\max(a,b),\min(a,b)\bigr)$
in increasing order. Because $s(a,b)\ge\max(a,b)$, only finitely many
pairs have any given slack (at most $\binom{S}{2}$ pairs have $s\le S$),
so the keys form a well-order of type~$\omega$: every pair has finitely
many predecessors. For each pair in turn, the staircase (i) \emph{raises}
its lag from the current value to $s(a,b)$ by oscillating $D\in\{0,1\}$
via R,L pairs (each pair bumps $\ell$ by~$2$ while returning $D$); (ii)
\emph{rides} right at fixed lag $s(a,b)$ until the displacement lands on a
recurring separator, which exists by Theorem~\ref{thm:bandrec}; (iii)
\emph{tails} right so displacements grow without bound across phases. Each
step keeps $D\ge 0$, so the trajectory is safe; it is separating by
construction and Yes-emitting because the lag $\to\infty$ and every cell
has infinitely many weak descents (\lean{weak\us descents\us infinite}).

\subsubsection{Unbounded above: the growing-lag staircase}
\label{sec:unbounded}

When $c$ is unbounded above, the band recurrence can fail
(Remark~\ref{rem:apsplit}), so a fixed-lag ride is impossible. The
replacement is a recurrence not at a fixed slack but at \emph{arbitrarily
large} even slack.

\begin{theorem}[Separators at arbitrarily large even slack;
  \lean{q2even}]\label{thm:q2even}
If $c$ is unbounded above, then for every distinct $a<b$ and every
prescribed lag floor~$L$ there exists an \emph{even} slack $s\ge L$ and a
displacement~$D$ with $[\,c(a{+}D)<D{+}s\,]\neq[\,c(b{+}D)<D{+}s\,]$.
\end{theorem}
\begin{proof}[Proof idea]
Suppose not: for some floor~$L$ \emph{no} even slack $s\ge L$ separates at
any~$D$ (the ``bad adversary''). Writing the separation window at
displacement~$D$ as $(\min v_a,v_b,\ \max v_a,v_b]$ with $v_a=a+g(a{+}D)$,
$v_b=b+g(b{+}D)$, the adversary says this window contains no even integer
$\ge L$. Whenever the window's top exceeds~$L$, this forces it to have
\emph{width exactly $1$} with an \emph{odd} top
(\lean{width1\us of\us no\us even\us in\us window}); width~$1$ at a link
means $|c(a{+}D)-c(b{+}D)|=1$, which (since $b{+}D=a{+}(D{+}\delta)$,
$\delta=b-a$) shows $g$ does not decrease going left by~$\delta$ at any
high link. Unboundedness above provides, for any~$M$, a \emph{spike} cell
on the $a$-ray with $g\ge M$ (\lean{phi\us spike\us on\us a\us ray}).
Walking left in steps of~$\delta$ from the spike
(\lean{leftward\us walk}), $g$ stays $\ge M$ all the way down to a fixed
\emph{chain-start} cell in $\{a,\dots,a{+}\delta{-}1\}$. But $g$ on those
finitely many fixed cells is bounded by a constant; choosing $M$ above
that constant yields a contradiction. Hence the bad adversary is
impossible and the separators exist at every lag floor. (A residue
pigeonhole over the $\delta$ chains makes the spike-to-chain-start step
fully rigorous for all $\delta$.)
\end{proof}

\paragraph{The growing-lag staircase.}
The unbounded trajectory (\lean{GrowStair\dt gmoves}) enumerates the
pairs (\lean{nthPair}) and processes them in turn. For the $i$-th phase it
requests, via Theorem~\ref{thm:q2even}, a separator at an even slack
\emph{at least} the current lag plus a margin (the lag floor
\lean{Lfloor}~$i$, which strictly exceeds all prior lags). It then
walks/oscillates to raise the lag to that slack and rides right to the
separator, always keeping $D\ge 0$. Because each phase's requested slack
exceeds the previous lag, the lags strictly increase across phases and the
$\omega$-enumeration of pairs is well-founded. Safety
(\lean{grow\us safe}), separation (\lean{grow\us separates}), and
Yes-emission (\lean{grow\us yes}) are proved exactly as before and fed to
the reduction. This is the only place a \emph{growing} lag is essential;
it is what catches the climbing separators of the AP-split example
(Remark~\ref{rem:apsplit}).

\begin{remark}[Why a fixed trajectory suffices after all]
Earlier we believed the winner had to be a genuinely adaptive
belief-state machine, because no single fixed move schedule could
reconcile catching low-slack discriminators (which want $D\approx t$) with
bounding $k_0$ (which wants $D$ small). The resolution is that the
\emph{moves} can be fixed (signal-independent) provided the trajectory is
tailored to~$c$: its slacks and ride lengths depend on~$c$, but it is a
single predetermined sequence, which is exactly what the reduction
consumes. Lag monotonicity is then not an obstruction but an organising
principle---the staircase simply visits separators in nondecreasing slack
order.
\end{remark}

% ------------------------------------------------------------
\subsection{Two complete winning families (locally decodable)}
\label{sec:local}

Before the general construction, we record two natural families that win
by a much simpler, fully self-contained strategy: a fixed-amplitude
oscillation. These were the first parts of the winning direction we
formalised, and they remain the cleanest worked instances.

\begin{definition}[Locally $K$-decodable]\label{def:k}
$c$ is \emph{locally $K$-decodable} if the tuple of first-\Yes{} times at
displacements $0,1,\dots,K{-}1$---equivalently the bucketed tuple
$(c(k_0),\dots,c(k_0{+}K{-}1))$---determines~$k_0$.
\end{definition}

\begin{theorem}[$K$-decodable winning theorems; \lean{LocallyDecodable\us BobWins}
  ($K{=}2$), \lean{LocallyK3Decodable\us BobWins} ($K{=}3$),
  \lean{LocallyKDecodable\us BobWins} (any~$K$)]\label{thm:k}
If $c$ is locally $K$-decodable, Bob wins, via a period-$2K$ triangle-wave
sweep of displacements $0,1,\dots,K{-}1,\dots,1,0,\dots$ with $K$
detectors (first \Yes{} at each displacement). The displacement stays in
$[0,K{-}1]$, so safety is automatic.
\end{theorem}

\begin{example}[Adjacent transpositions; \lean{adjPerm\us BobWins}]\label{ex:adj}
Let $c$ swap $2i{-}1\leftrightarrow 2i$ for every $i$ (so $c(n)=n+1$ if
$n$ is odd, $c(n)=n-1$ if $n$ is even). Here $g(n)=c(n)-n\in\{+1,-1\}$ is
bounded, yet $c$ is not eventually identity; Bob wins by local decoding at
$K{=}2$. This already shows the boundary is non-EI, not unbounded growth
of~$g$.
\end{example}

\begin{example}[Forward block $3$-cycle; \lean{blkPerm\us BobWins}]\label{ex:blk}
Let $c(m)=m+1$ unless $3\mid m$, in which case $c(m)=m-2$ (each block
$\{3b{+}1,3b{+}2,3b{+}3\}$ rotates forward). Then $c(m)\le m+1$ always, so
\emph{no} rightward probe ever separates the candidates by navigation
alone. But cell~$3$ wakes early, at time~$2$ (since $c(3)=1$), which
splits $k_0=1$ from $k_0=2$ at displacement~$1$: at $(D{=}1,t{=}3)$ the
world $k_0=2$ stands on the already-awake cell~$3$ and reports \Yes, while
$k_0=1$ stands on cell~$2$, still asleep, and reports \No. The $K{=}3$
sweep, monitoring small displacements from time~$0$, catches this early
wake-up, so $c$ is locally $3$-decodable and Bob wins.
\end{example}

\begin{remark}[The fixed-$K$ architecture cannot cover all non-EI~$c$]
\label{rem:k5fail}
The locally-$K$-decodable classes (any fixed amplitude~$K$, formalised in
Lean for arbitrary $K$) are genuinely incomplete. The \emph{block-$5$
cycle}---rotate each $\{5b{+}1,\dots,5b{+}5\}$ forward---is non-EI, yet for
\emph{every} $K$ some pair of start cells produces an identical
$K$-tuple of first-\Yes{} times: the colliding pair $(4,5)$ has
$c(4..9)=(5,1,7,8,9,10)$ and $c(5..10)=(1,7,8,9,10,6)$, which a coarse
amplitude-$K$ sweep cannot tell apart even though Theorem~\ref{thm:lemmaA}
guarantees them distinguishable (here at $D{=}0$, $t{=}2$: $c(4)=5\ge 2$
but $c(5)=1<2$). A single fixed-amplitude trajectory cannot finely monitor
all displacements at once. This is precisely why the general winning
direction needs the $c$-tailored staircase of Section~\ref{sec:dichotomy},
whose lag varies with the pair.
\end{remark}

% ------------------------------------------------------------
\subsection{Worked examples table}
\label{sec:table}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\textwidth}{@{}l l c X@{}}
\toprule
\textbf{Permutation $c$} & \textbf{EI?} & \textbf{Bob} & \textbf{Reason} \\
\midrule
Identity $c(n)=n$
  & yes & \textbf{loses}
  & $(2j,\,2j{+}1)$ collide: only even thresholds probeable
    (Rem.~\ref{rem:flat}). \\
Finitary (any finite-support perm.)
  & yes & \textbf{loses}
  & EI with $N=$ support bound; same collision for large $k_0$. \\
Adjacent transpositions
  & no & \textbf{wins}
  & locally $2$-decodable (Ex.~\ref{ex:adj}); bounded $g$;
    bounded-above staircase. \\
Forward block $3$-cycle
  & no & \textbf{wins}
  & locally $3$-decodable (Ex.~\ref{ex:blk}); bounded above. \\
Parity-shift $c(2k){=}2k{+}2,\,c(2k{+}1){=}2k{-}1$
  & no & \textbf{wins}
  & bounded above ($g\in\{\pm2\}$); fixed-lag staircase
    (\S\ref{sec:bounded}). \\
AP-split (Rem.~\ref{rem:apsplit})
  & no & \textbf{wins}
  & unbounded above; growing-lag staircase (\S\ref{sec:unbounded});
    no fixed lag works. \\
Any non-EI $c$
  & no & \textbf{wins}
  & dichotomy of \S\ref{sec:dichotomy} (Thm.~\ref{thm:main}). \\
\bottomrule
\end{tabularx}
\caption{Outcomes by family. Eventually-identity $\iff$ Bob loses; the
two non-EI regimes (bounded/unbounded above) are won by the
corresponding staircase.}
\end{table}

The two italicised non-EI rows are the instructive boundary cases. The
\emph{parity-shift} permutation has displacement $g(n)\in\{+2,-2\}$,
constant on residues---a balanced, non-eventually-identity permutation
that is bounded above; it is exactly the kind of example that defeats
naive cut-counting arguments, yet the bounded-above staircase wins it
with a single fixed lag. The \emph{AP-split} permutation
(Remark~\ref{rem:apsplit}) is its unbounded-above counterpart: there
\emph{no} fixed lag recurs, and only the growing-lag staircase succeeds.

% ============================================================
\section{Machine-verified formalisation in Lean~4}
\label{sec:lean}

To provide the highest degree of certainty, the entire argument is
formalised and machine-checked in \textbf{Lean~4} (with Mathlib) in the
development \lean{griddles-p4-lean}. The headline is unconditional:

\begin{quote}
\itshape
The main theorem \lean{main}, namely
\lean{BobWins~c~$\leftrightarrow$~$\lnot$~EventuallyIdentity~c},
is \textbf{fully \lean{sorry}-free}, and its axiom audit
\[
  \verb|#print axioms main| \;=\; [\verb|propext|,\ \verb|Classical.choice|,\ \verb|Quot.sound|]
\]
lists \emph{only} the three standard foundational axioms, with \emph{no}
\verb|sorryAx|. The full library builds cleanly with \lean{lake build}
(3338~jobs).
\end{quote}

``Axiom-clean'' means precisely this: the \verb|#print axioms| audit shows
only \verb|propext|, \verb|Classical.choice|, and \verb|Quot.sound|. Every
component theorem below is individually axiom-clean, and so is their
combination in \lean{main}.

\subsection{Module structure}

The library is organised in dependency order:

\begin{enumerate}[leftmargin=2em,topsep=2pt,itemsep=2pt]
  \item \textbf{\lean{Defs}} --- Game semantics: \lean{Perm},
  \lean{Strategy}, \lean{Action}, the trajectory functions
  (\lean{state}/\lean{position}/\lean{history}/\lean{displacement}), and
  the predicates \lean{WinsFrom}, \lean{BobWins}, \lean{EventuallyIdentity}.
  \item \textbf{\lean{Parity}} --- The parity lock
  ($t-D_t$ even) and the position lower bound.
  \item \textbf{\lean{Losing}} --- The entire losing direction:
  \lean{parallel\us evolution}, \lean{signal\us match}, and the headline
  \lean{EventuallyIdentity\us loses}.
  \item \textbf{\lean{LemmaA}} --- The distinguishability theorem
  (Theorem~\ref{thm:lemmaA}), including the $\Delta\ge2$ crux by
  forward-closure (\lean{indist\us Delta\us ge\us 2\us impossible}) and
  the $\Delta=1$ shift (\lean{shift\us one\us implies\us eventually\us
  identity}); the corollary \lean{not\us eventually\us identity\us
  implies\us distinguishable}.
  \item \textbf{\lean{WinningRestricted} / \lean{WinningKProbe} /
  \lean{WinningKProbeGen}} --- The locally $K$-decodable winning classes
  (Theorem~\ref{thm:k}) for $K{=}2$, $K{=}3$, and arbitrary~$K$, with
  worked examples \lean{adjPerm}, \lean{cyc3Perm}, \lean{blkPerm}.
  \item \textbf{\lean{WinningAdaptive}} --- The reduction infrastructure:
  \lean{separatorAtSlack} and \lean{separatorAtSlack\us iff\us window};
  the bijectivity engines \lean{weak\us descents\us infinite} and
  \lean{weak\us ascents\us infinite}; first-Yes finiteness
  (\lean{yesSet\us finite}); the reduction \lean{ofMoves\us localizes} and
  \lean{localizes\us BobWins}; the \lean{BandProvider} interface and the
  band-top staircase \lean{BobWins\us of\us bandProvider}.
  \item \textbf{\lean{BoundedAbove}} --- The cut lemma
  (Lemma~\ref{lem:cut}): \lean{descent\us cofinite\us imp\us EI} and its
  mirror \lean{ascent\us cofinite\us imp\us EI}.
  \item \textbf{\lean{BandBoundedAbove}} --- The bounded-above band
  recurrence \lean{band\us of\us boundedAbove}
  (Theorem~\ref{thm:bandrec}), packaged as
  \lean{bandProvider\us of\us boundedAbove} and the bounded-above winner
  \lean{notEI\us boundedAbove\us BobWins}.
  \item \textbf{\lean{Q2Even}} --- The unbounded-above separator lemma
  \lean{q2even} (Theorem~\ref{thm:q2even}), via the leftward-chain and
  residue-pigeonhole argument.
  \item \textbf{\lean{GrowStair}} --- The growing-lag staircase
  (\lean{gmoves}) and the unbounded-above winner
  \lean{notEI\us unboundedAbove\us BobWins}.
  \item \textbf{\lean{Answer}} --- The main theorem \lean{main}, and the
  consistency capstone \lean{locallyDecodable\us main}.
\end{enumerate}

\subsection{The main theorem}

The capstone, stated and proved in Lean~4, dispatches the winning
direction on bounded-above:

\begin{quote}
\lean{theorem main (c : Perm) : BobWins c $\leftrightarrow$ $\lnot$ EventuallyIdentity c}
\end{quote}

The forward implication ($\to$) is the losing direction
(\lean{EventuallyIdentity\us loses}). The reverse ($\leftarrow$) does a
\lean{by\us cases} on whether $c$ is bounded above
($\exists B,\ \forall n,\ (c\,n)\le n+B$): the bounded-above branch calls
\lean{notEI\us boundedAbove\us BobWins} (the fixed-lag staircase fed by
\lean{band\us of\us boundedAbove}), and the unbounded-above branch calls
\lean{notEI\us unboundedAbove\us BobWins} (the growing-lag staircase fed
by \lean{q2even}). Both branches route through the axiom-clean reduction
\lean{localizes\us BobWins}, so the whole proof is \lean{sorry}-free.

\subsection{The journey, and what made it work}

The proof was not found in this clean shape. Three milestones reshaped it.
First, the distinguishability crux (Theorem~\ref{thm:lemmaA},
$\Delta\ge2$) was long believed to need global surjectivity bookkeeping or
a delicate even/odd case split; it fell to the elementary forward-closure
argument (Lemma~\ref{lem:badprop}), which turns the active set on each
residue into a finite prefix. Second, the winning direction was initially
reduced to a single combinatorial ``band recurrence'' lemma believed true
and computationally well-supported. That belief was \emph{wrong}: the
AP-split permutation (Remark~\ref{rem:apsplit}) is an explicit
counterexample, and recognising this was the turning point. Third, the
band recurrence's failure is \emph{structural}, not accidental---it fails
exactly when $c$ is unbounded above---which yielded the clean dichotomy of
Section~\ref{sec:dichotomy}. The bounded-above regime keeps the original
fixed-lag staircase (now resting on the cut lemma rather than the false
recurrence); the unbounded-above regime gets a growing-lag staircase fed
by \lean{q2even}, proved via a leftward-chain and residue-pigeonhole
argument. Crucially, no adaptive belief-state strategy was ever
needed---a fixed, $c$-tailored, signal-independent trajectory suffices in
both regimes, which is precisely what the candidate-collapsing reduction
(Theorem~\ref{thm:reduction}) consumes.

% ============================================================
\section*{Acknowledgements}
\addcontentsline{toc}{section}{Acknowledgements}
% ============================================================

The solution to this puzzle --- including the characterisation of Bob's
winning condition, the distinguishability theorem, the discovery that the
band recurrence is false, and the bounded/unbounded staircase dichotomy
--- was developed in collaboration with
\href{https://claude.ai}{Claude} (Anthropic).  The simulation and
game-solver code, the Lean~4 formalisation, and this writeup were
produced in collaboration with
\href{https://claude.ai/code}{Claude Code} (Anthropic).

% ============================================================
\section*{References}
% ============================================================

{\small

[1] The Mathlib Community (2020).
\textit{Lean 4 Mathematical Library (\textsc{Mathlib4})}.
\url{https://leanprover-community.github.io}.

[2] de Moura, L.\ \& Ullrich, S.\ (2021).
The Lean~4 Theorem Prover and Programming Language.
\textit{CADE~28}, LNCS \textbf{12699}, 625--635.

}

% ============================================================

\end{document}
