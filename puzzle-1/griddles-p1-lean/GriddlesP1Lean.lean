-- Root of the GriddlesP1Lean library.
-- Submodules (in dependency order):
--   Defs          — core types and definitions
--   Separability  — every proper coloring is separable (f(r,c) = base + R(c) + D(r))
--   Slopes        — slope extension and palindrome machinery
--   Bijections    — bijections for fixed-point counting
--   Counting      — cardinality results and Burnside's lemma
--   Answer        — the n=101 answer and general formula
import GriddlesP1Lean.Defs
import GriddlesP1Lean.Separability
import GriddlesP1Lean.Slopes
import GriddlesP1Lean.Bijections
import GriddlesP1Lean.Counting
import GriddlesP1Lean.Answer
